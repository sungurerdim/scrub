{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the window is allowed to ask for. The nine commands below are the whole of it: each is one stage of the pipeline or one question about an artifact, and nothing here reaches the filesystem except through the pipeline, which refuses to overwrite and never deletes. The dialog is how somebody chooses what to scan, and it grants nothing beyond returning the path they picked.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "allow-begin",
    "allow-scan",
    "allow-analyze",
    "allow-groups",
    "allow-copies",
    "allow-plan",
    "allow-preflight",
    "allow-apply",
    "allow-undo"
  ]
}
