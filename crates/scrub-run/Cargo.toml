[package]
name = "scrub-run"
description = "Drives each stage of the pipeline, so every interface runs the same one"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
scrub-core = { path = "../scrub-core" }
scrub-platform = { path = "../scrub-platform" }
scrub-store = { path = "../scrub-store" }
serde_json.workspace = true
jiff.workspace = true

[lints]
workspace = true

[dev-dependencies]
tempfile.workspace = true
